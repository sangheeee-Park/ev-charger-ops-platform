package com.example.backend.chargingstation.repository;

import com.example.backend.chargingstation.entity.RegionDetailCode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegionDetailCodeRepository extends JpaRepository<RegionDetailCode, String> {
}
